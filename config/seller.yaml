# Who this install coaches. This tracked file ships EMPTY on purpose: until the profile is filled,
# every page leads to /setup and no call can be started or imported.
#
# Do not edit this file. Fill the form at http://127.0.0.1:8140/setup, which writes your own copy
# to <data>/settings/seller.yaml (gitignored; SALESCOACH_SETTINGS moves it). That copy wins, key by key.
#
# The fields, as they would look filled in:
#
# name: Priya Nair                      # required
# emails: [priya@acmecloud.com]         # required, every address you send from or are invited on
# company: Acme Cloud                   # required
# offering: HR software that replaces spreadsheets for hourly staff      # required: what you sell
# role: Account executive
# website: www.acmecloud.com
# icp: US mid-market companies, 200 to 2,000 employees                   # who you sell to
# buyer_titles: HR directors and CFOs
# vocabulary: shift, roster, timesheet  # plain words your buyers use; the coach writes in them
# own_domains: [acmecloud.com]          # colleagues' domains; default: the domains of `emails`
# aliases: []                           # other names a transcript may use for you
# languages: [en]                       # languages on your calls, main one first (en, hi, es ...)
# timezone: America/New_York            # default Asia/Kolkata
# signature: |                          # the sign-off the email drafter must use
#   Best,
#   Priya
# call_context: >                       # free text: how you sell, deal size, cycle, who joins your calls
#   Deals take two to three months and always involve finance.
name: ""
emails: []
company: ""
offering: ""
